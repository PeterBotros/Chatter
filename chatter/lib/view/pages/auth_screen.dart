import 'package:flutter/material.dart';

// Placeholder AuthScreen (no Firebase)
class AuthScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Auth')), // Placeholder
      body: Center(child: Text('Auth screen (Firebase removed)')),
    );
  }
}
